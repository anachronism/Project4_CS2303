#ifndef ITEM_H
#define ITEM_H
#include "global.h"
/*
class Item{
	public:
		string name;
		Item();
		int id_compare(Item *currentNode, Item *newNode);
	private:
		StorePtr *stores;
		Item *left;
		Item *right;
};

typedef Item *ItemPtr; */

#endif
